# Local signing keys — NEVER commit private material
.local-secrets/

/target
**/*.rs.bk
*.swp
.DS_Store

# Local secrets / runtime config — never commit (Web-mode auth, TURN creds)
*.local.toml
.env

# test artifacts
/.playwright-mcp/
/qcast-*.png
/qcast-*.jpeg
